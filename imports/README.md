# Job Import Files

This folder is for local portal exports or pasted search results.

Do not commit real import files. CSV and JSON files in this folder are ignored because they may reveal your search history or target companies.

Use this CSV header:

```csv
title,company,location,url,description,requirements,job_id
```
